{
  "name": "my-react-project",
  "private": true,
  "workspaces": [
    "packages/app",
    "components/time"
  ],
  "devDependencies": {
    "typescript": "^4.5.0",
    "vite": "^2.6.0"
  },
  "scripts": {
    "build": "pnpm --filter @package/app build",
    "dev": "pnpm --filter @package/app dev"
  }
}